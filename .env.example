# ==================================
# WatchParty Environment Variables
# ==================================

# Optional: Server port (default: 3000)
PORT=3000

# Optional: PostgreSQL password (default: watchparty)
# Change this in production!
POSTGRES_PASSWORD=watchparty

# Optional: PostgreSQL connection URL
# Override if not using Docker defaults
# DATABASE_URL=postgresql://watchparty:watchparty@postgres:5432/watchparty

# Optional: Redis connection URL
# Override if not using Docker defaults
# REDIS_URL=redis://redis:6379

# Optional: Admin password for protecting the admin interface
# If not set, admin routes are open (only use in development!)
ADMIN_PASSWORD=

# Optional: Set to "true" if using HTTPS (enables secure cookies)
# Leave empty or "false" for HTTP deployments
SECURE_COOKIES=

# Optional: Plex Client Identifier (for OAuth)
# Leave blank to use default
# NEXT_PUBLIC_PLEX_CLIENT_ID=watchparty-app
